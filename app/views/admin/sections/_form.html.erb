<%= form_for(@section,url: yield(:admin_path)) do |f| %>

  <%= f.hidden_field :course_id, :value => params[:course_id] || @section.course_id %>

	<%= f.label :title,"タイトル" %>
	<%= f.text_field :title %>

	<%= f.label :introduce,"説明文"%>
	<%= f.text_area :introduce %>

	<%= f.submit yield(:button_text), class: "btn btn-primary" %>

<% end %>
