<% provide(:title, "Edit user")%>
<% provide(:button_text, "ユーザー情報を更新する")%>

<h1>ユーザー情報を更新する。</h1>
  <% if current_user.admin? %>
    <%= link_to "削除する",@user, method: :delete,
                                  data: { confirm: "本当に削除しますか？" },class: "btn btn-danger" %>
  <% end %>

<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<%= render 'form' %>
	</div>
</div>


